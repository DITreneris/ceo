# DEPRECATED — GitHub Pages. Production is Vercel only (vercel.json, api/*).
# Do not use as a buyer-facing host. Unpublish Pages in GitHub Settings if still live.
# Merge gate: .github/workflows/ci.yml

name: Deploy to GitHub Pages (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  skipped:
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Pages deploy is disabled. Production is Vercel."
